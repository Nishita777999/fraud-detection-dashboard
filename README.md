# fraud-detection-dashboard
This project is a full-stack Machine Learning web application that detects fraudulent transactions in real-time.  It uses a RandomForest classifier trained with scikit-learn and exposes predictions through a FastAPI backend. A simple HTML + JavaScript frontend allows users to input transaction details and receive predictions instantly.
